# TASK-006 — Add Vitest Coverage Thresholds

## TASK
TASK-006: Add @vitest/coverage-v8 and configure coverage thresholds (lines 80, branches 80) in vitest.config.ts.

## Summary
Installed `@vitest/coverage-v8` as a devDependency and updated `vitest.config.ts` to enable the v8 coverage provider with `text` and `lcov` reporters and 80% thresholds for lines and branches. All existing tests continue to pass and coverage output is confirmed working.

## Allowed dirs honored
YES

## Files changed
- `/Users/tonyrosario/dev/poc/claude-multi-agent/vitest.config.ts` — added `coverage` block (provider, reporter, thresholds)
- `/Users/tonyrosario/dev/poc/claude-multi-agent/package.json` — `@vitest/coverage-v8: ^4.0.18` added to devDependencies (via npm install)
- `/Users/tonyrosario/dev/poc/claude-multi-agent/package-lock.json` — updated automatically by npm

## Tests added/updated
No new test files required. The task is a tooling/config change. Validation was performed by running the full test suite with coverage enabled.

## Commands run + results

```
npm install -D @vitest/coverage-v8
# added 18 packages, 0 vulnerabilities

npm run test -- --coverage
# 1 test file, 12 tests — all passed
# Coverage: 100% stmts / 100% branch / 100% funcs / 100% lines (exceeds 80% thresholds)
# Text report printed to stdout; lcov report written to coverage/
```

## Commits proposed (Conventional Commits)
```
chore(config): add vitest coverage thresholds via @vitest/coverage-v8
```

## Edge cases handled
- Threshold values set to 80% as specified; current codebase is at 100%, so thresholds will not cause false failures.
- Both `text` and `lcov` reporters configured — `lcov` enables CI/CD integration (e.g. Codecov, SonarQube).

## Risks / TODOs
- If new source files are added without corresponding tests, coverage may drop below 80% and `npm run test -- --coverage` will fail. This is intentional and desirable per the task requirements.
- The `coverage/` output directory (generated by vitest) should be added to `.gitignore` if not already present.

## Contract deviations
NONE
